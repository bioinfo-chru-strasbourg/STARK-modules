SIFT_seq_submit2.pl
	Want to force jobs submitted by libwww-perl into the queue even
	if no email address is supplied. This info is in the
	environment var HTTP_USER_AGENT =~ m/libwww/


perl /home/slevy/code/COMP/scripts/merge-gff.pl -G1=<infile> -G2=<outfile> -F1=3

IntersectLocations.sh  <file1> gff <file2> gff simple >  <outfile>

